import React from "react";

const Header = () => {
  return (
    <div className="header">
      <h1>Memory match</h1>
      <h3>Try not to select the same card twice!</h3>
    </div>
  );
};

export default Header;
